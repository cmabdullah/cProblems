//underconstruction


#include<stdio.h>
#include<conio.h>
#include<stdio.h>
int minpos(float[]. int);
void main(){
    int n:
        float x[[5]= {2 ,3, 1, 6, 7};

        printf("enter the value of n :");
        scanf("%d",  &n);
        if (n>=1&& n<=10)
            :
        else{
            printf("invalid value of n... Pres any key to terminate the program ..");

            getch():
            exit(0);
        }

        printf("Write the first %d elements of arrey, the first minimum value is stored at index %d". n , minpos(x , n));
        getch():

}
int minpos(float a[] , int N){
int i . index;
    float min-999.99;
    for (i = 0; i<N ; i++)
    if (a[i]<min){
        min-a[i];
        index = i;
    }
    return(index);

}
